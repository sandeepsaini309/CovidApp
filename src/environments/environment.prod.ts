export const environment = {
  production: true,
};

// !! build cmd
// ng build --configuration=production --base-href "https://sandeepsaini309.github.io/CovidApp/"

// !! Deploy cmd
// npx angular-cli-ghpages --dir=dist/covidApp
